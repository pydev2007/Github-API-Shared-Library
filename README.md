# Discovery Pipeline
A simple shared library with a pipeline to check any commits are made after a release.

## Additional Notes

Repos may need to be specified inside the the Github App creds in Jenkins. Change the repo access strategy to "Specify accessible repositories" in Jenkins if it throws an access error in: your credential > advanced > Repository access strategy. Then, list your repositories in the repo section below.
